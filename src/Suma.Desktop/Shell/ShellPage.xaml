<Page
    x:Class="Suma.Desktop.Shell.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{StaticResource SumaBackgroundBrush}"
    RequestedTheme="Light"
    mc:Ignorable="d">

    <NavigationView
        x:Name="ShellNavigation"
        IsBackButtonVisible="Collapsed"
        IsPaneToggleButtonVisible="False"
        IsSettingsVisible="True"
        ItemInvoked="OnNavigationItemInvoked"
        OpenPaneLength="248"
        PaneDisplayMode="Left"
        SelectionFollowsFocus="Disabled">

        <NavigationView.PaneHeader>
            <StackPanel Padding="8,24,8,20" Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Ellipse Width="8" Height="8" Fill="{StaticResource SumaAccentBrush}" />
                    <TextBlock Style="{StaticResource SumaWordmarkTextStyle}" Text="{x:Bind ViewModel.ApplicationTitle}" />
                </StackPanel>
                <TextBlock Style="{StaticResource SumaCaptionTextStyle}" Text="{x:Bind ViewModel.ApplicationTagline}" />
            </StackPanel>
        </NavigationView.PaneHeader>

        <NavigationView.MenuItems>
            <NavigationViewItem AutomationProperties.Name="Overview" Content="Overview" Tag="Overview">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE80F;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem AutomationProperties.Name="Activity" Content="Activity" Tag="Activity">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE9D2;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem AutomationProperties.Name="Planning" Content="Planning" Tag="Planning">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE8A5;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <Frame x:Name="ContentFrame" />
    </NavigationView>
</Page>
